      // export function matchmaking();
      // filter requests by subject
      // retrieve userInfo
      // sc.decode() then JSON.parse(obj) OR JSON.parse(sc.decode(obj))
      // place user in queue
      // when queue full send matchObj