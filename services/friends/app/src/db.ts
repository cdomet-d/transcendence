import sqlite3 from 'sqlite3';

export async function addFriendRequest(fromUser: string, toUser: string) {
  /*  const db = await mysql.createConnection({ config here });
  // Insert the unvalidated friend request row
  await db.execute(
    `INSERT INTO friend_requests (sender, receiver, status) VALUES (?, ?, 'pending')`,
    [fromUser, toUser]
  );
  await db.end();*/
}